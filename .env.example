# ===========================================
# SECURE WEB GATEWAY - Environment Configuration
# ===========================================
# Copy this file to .env and customize:
#   cp .env.example .env
#
# On first 'make run', GRAFANA_PASSWORD will be
# automatically replaced with a secure random value.
# ===========================================

# Custom Certificates Path (Optional)
# Default: ./certs (in project directory)
# CERTS_PATH=./certs

# Email for Let's Encrypt certificates
GATEWAY_EMAIL=admin@example.com

# Project Name for Docker Compose
COMPOSE_PROJECT_NAME=hardened_gateway

# Monitoring Domain (leave empty to disable Grafana access via gateway)
MONITORING_DOMAIN=monitor.example.com

# ===========================================
# SECURITY: IP Whitelist for Monitoring (SEC-005)
# ===========================================
# Comma-separated list of IP ranges allowed to access monitoring
# Leave empty to allow all IPs (not recommended for production)
# Examples: 
#   Single IP:     MONITORING_ALLOWED_IPS=203.0.113.50
#   CIDR range:    MONITORING_ALLOWED_IPS=10.0.0.0/8
#   Multiple:      MONITORING_ALLOWED_IPS=10.0.0.0/8,192.168.0.0/16,203.0.113.0/24
MONITORING_ALLOWED_IPS=

# ===========================================
# SECRETS (Do not commit .env to git!)
# ===========================================

# CrowdSec Bouncer API Key
# Generate with: docker exec gateway_crowdsec cscli bouncers add gateway-bouncer
CROWDSEC_BOUNCER_API_KEY=

# Grafana Admin Password
# WARNING: This will be auto-replaced with a secure password on first 'make run'
GRAFANA_PASSWORD=admin
